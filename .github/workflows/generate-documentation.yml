name: Generate Documentation

on:
  push:
    branches:
      - master
      
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:    

      - name: Build
        uses: andstor/jsdoc-action@v1
        with:
          source_dir: ./api
          recurse: true
          output_dir: ./docs
